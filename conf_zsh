
# edit command line with vim by running ctrl-e
autoload -Uz edit-command-line
zle -N edit-command-line
bindkey ^e edit-command-line

eval "$(starship init zsh)"
eval "$(atuin init zsh)"

alias ll="ls -lah"
alias vim=nvim

